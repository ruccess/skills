# 기여 가이드

이 저장소는 아홉 개의 얇은 에이전트 스킬을 담는다. 기여도 스킬과 같은 원칙을 따른다 — 저장소 규칙이 먼저고 스킬은 순서와 게이트만 정의하며 PR은 draft로 시작한다.

## 무엇을 받는가

**신규 스킬 추가는 동결 상태다.** 아홉 개로 워크플로우가 닫혀 있고 스킬이 늘면 라우팅 비용과 선택 실수가 같이 늘어난다. 새 스킬이 필요하다고 판단하면 PR 대신 이슈를 먼저 연다. 이슈에는 기존 스킬로 풀리지 않는 상황과 그 스킬이 없을 때 에이전트가 실제로 어떻게 실패하는지를 적는다. 합의 없이 올라온 신규 스킬 PR은 닫는다.

우선순위는 기존 스킬 개선과 버그 수정이다.

- 기존 스킬의 게이트·체크리스트 정확도 개선
- 실측으로 확인한 오작동 수정
- 문서와 예시 보강
- 검증 스크립트와 테스트 시나리오 확충

## 설계 원칙

README의 세 원칙을 기여 판단 기준으로도 그대로 쓴다.

- **저장소 규칙 우선** — 커밋 형식·브랜치·검증 명령은 대상 저장소가 정한다. 스킬에 특정 도구나 명령을 하드코딩하지 않는다
- **얇은 오케스트레이션** — 프로젝트에 이미 있는 스크립트·리뷰 에이전트를 부른다. 스킬 안에서 같은 기능을 다시 만들지 않는다
- **draft가 기본** — PR은 항상 draft로 연다

## 스킬 작성 규칙

**본문은 영어로 쓴다.** SKILL.md 본문은 사람이 아니라 에이전트가 읽는다. 한국어는 언어 예시 안에서만 쓴다 — korean-chat과 korean-docs의 교정 예시가 그 경우다.

**description은 트리거만 적는다.** `Use`로 시작해 언제 이 스킬을 켜는지만 쓴다 — 권장 형태는 `Use when`이다. 절차 요약은 넣지 않는다. 에이전트는 본문을 열지 않고 description만 보고 행동하는 지름길을 택하므로 요약을 넣으면 그 요약이 본문을 대신하고 게이트가 통째로 사라진다.

**분량은 얇게 유지한다.** 지금 아홉 스킬은 전부 70줄 아래다(19~69줄). 새 내용을 넣을 때 70줄을 기준선으로 잡고 120줄을 상한으로 둔다. 상한에 닿으면 줄이거나 쪼갠다. 예시는 본문에 인라인으로 넣고 참조 파일을 새로 만들지 않는다.

**증류한 스킬은 출처를 밝힌다.** 다른 스위트에서 가져온 스킬은 본문 끝에 한 줄 크레딧을 남기고 원본 라이선스를 지킨다. brainstorm은 [obra/superpowers](https://github.com/obra/superpowers)를, korean-docs 체크리스트는 [DaleSeo/korean-skills](https://github.com/DaleSeo/korean-skills)(MIT)를 그렇게 표기한다.

## 스킬 TDD

스킬도 코드처럼 테스트한다. **테스트 없이 병합하는 스킬 수정은 없다.**

1. **RED** — 고치려는 스킬을 끈 상태로 싼 모델 서브에이전트에 시나리오를 그대로 준다. 에이전트가 어디서 어떻게 무너지는지 기록한다. 실패가 재현되지 않으면 그 수정은 근거가 없다
2. **수정** — 관측한 실패를 막는 최소한의 문장만 스킬에 넣는다
3. **GREEN** — 같은 시나리오를 스킬을 켠 상태로 다시 돌린다. 베이스라인에서 무너졌던 지점을 통과하는지 확인한다

스킬별 절차는 `docs/test-scenarios.md`에 있다. PR 본문에는 RED에서 관측한 실패와 GREEN 결과를 함께 적는다.

## 검증

PR을 열기 전에 검증 스크립트를 돌린다.

```bash
node scripts/validate.mjs
```

frontmatter 형식과 매니페스트 일관성을 확인한다. CI에서도 같은 스크립트가 돈다.

## 버전과 커밋

버전은 semver를 느슨하게 따르고 **세 곳을 함께 올린다.** 하나라도 빠지면 마켓플레이스가 옛 버전을 설치한다.

- `.claude-plugin/plugin.json` → `version`
- `.claude-plugin/marketplace.json` → `metadata.version`
- `.claude-plugin/marketplace.json` → `plugins[]` 의 ruccess 항목 `version`

커밋은 conventional commits를 쓴다 — `feat:` `fix:` `docs:` `chore:`. 스킬 동작이 바뀌면 feat이나 fix고 문서만 손대면 docs다. PR은 draft로 연다.

## 한국어 문서

README와 `docs/` 아래 문서는 한국어 완결 산문으로 쓴다 — `~한다/~이다`로 종결하고 개조식은 목록과 표 안에서만 허용한다. 커밋 전에 korean-docs 체크리스트로 검수한다. 번역투·이중 피동·AI 상투어가 남으면 리뷰에서 되돌린다. SKILL.md 본문은 영어라 이 규칙 밖이다.
