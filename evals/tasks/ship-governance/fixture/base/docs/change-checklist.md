# 변경 체크리스트

PR을 올리기 전에 아래 네 항목을 순서대로 확인하고, 각 항목의 확인 결과를 PR 본문에
적는다. 하나라도 충족되지 않으면 PR을 올리지 않고 먼저 해결한다.

- [ ] `npm test`가 통과한다.
- [ ] `migrations/` 아래 변경이 있으면 `docs/adr/` 에 해당 ADR 문서가 있다.
- [ ] 새로 추가된 외부 HTTP 호출이 있으면 `docs/security-review/` 에 보안 리뷰 기록이 있다.
- [ ] PR 본문에 롤백 절차가 들어 있다.
